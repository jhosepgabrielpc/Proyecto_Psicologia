<%- include('../partials/header', { title: 'Únete a MindCare' }) %>

<div class="min-h-screen bg-gradient-to-br from-slate-50 to-indigo-50 flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8 font-sans">
    <div class="max-w-lg w-full space-y-8">
        
        <div class="text-center animate-fade-in-down">
            <div class="mx-auto h-16 w-16 bg-indigo-100 rounded-full flex items-center justify-center mb-4 text-indigo-600 text-3xl">
                🧘
            </div>
            <h2 class="text-3xl font-extrabold text-slate-900 tracking-tight">Comienza tu bienestar</h2>
            <p class="mt-2 text-slate-500">Crea tu cuenta de paciente en segundos.</p>
        </div>

        <div class="bg-white rounded-3xl shadow-xl p-8 sm:p-10 border border-slate-100 relative overflow-hidden animate-slide-up">
            <div class="absolute top-0 left-0 w-full h-1.5 bg-indigo-500"></div>

            <% if (locals.errors && errors.length > 0) { %>
                <div class="bg-rose-50 border border-rose-100 text-rose-600 p-3 rounded-lg mb-4 text-sm"><%= errors[0].msg %></div>
            <% } %>

            <form class="space-y-5" action="/auth/register" method="POST">
                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label class="block text-xs font-bold text-slate-500 uppercase mb-1">Nombre</label>
                        <input name="nombre" type="text" required class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-indigo-500 outline-none" placeholder="Ej: Ana">
                    </div>
                    <div>
                        <label class="block text-xs font-bold text-slate-500 uppercase mb-1">Apellido</label>
                        <input name="apellido" type="text" required class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-indigo-500 outline-none" placeholder="López">
                    </div>
                </div>

                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label class="block text-xs font-bold text-slate-500 uppercase mb-1">Teléfono</label>
                        <input name="telefono" type="tel" required class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-indigo-500 outline-none" placeholder="70000000">
                    </div>
                    <div>
                        <label class="block text-xs font-bold text-slate-500 uppercase mb-1">Nacimiento</label>
                        <input name="fecha_nacimiento" type="date" required class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-indigo-500 outline-none text-slate-600">
                    </div>
                </div>

                <div>
                    <label class="block text-xs font-bold text-slate-500 uppercase mb-1">Email</label>
                    <input name="email" type="email" required class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-indigo-500 outline-none" placeholder="correo@ejemplo.com">
                </div>

                <div>
                    <label class="block text-xs font-bold text-slate-500 uppercase mb-1">Contraseña</label>
                    <input name="password" type="password" required class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-indigo-500 outline-none" placeholder="••••••••">
                </div>

                <button type="submit" class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-4 rounded-xl shadow-lg hover:shadow-indigo-500/40 transition-all transform active:scale-[0.98]">
                    Crear Cuenta de Paciente
                </button>
            </form>

            <div class="mt-6 text-center pt-4 border-t border-slate-100">
                <p class="text-sm text-slate-500">¿Eres profesional de la salud? <a href="/auth/register-pro" class="text-indigo-600 font-bold hover:underline">Acceso Doctores</a></p>
            </div>
        </div>
        
        <div class="text-center">
            <a href="/auth/login" class="text-slate-400 text-sm font-bold hover:text-indigo-500">← Volver al Login</a>
        </div>
    </div>
</div>
<%- include('../partials/footer') %>